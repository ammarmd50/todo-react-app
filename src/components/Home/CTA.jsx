
function CTA({onGetStarted}) {
  return (
    <section className="cta">
      <h2>Start Organizing Today</h2>
      <button onClick={onGetStarted}>Go to App</button>
    </section>
  );
}

export default CTA;
